---
import Layout from "../../layouts/Layout.astro";
---

<Layout title="Verifikasi Email - NexOne">
    <div
        class="min-h-screen flex items-center justify-center p-4 md:p-6 bg-gradient-to-br from-gray-50 via-gray-100 to-gray-50 font-sans"
    >
        <div
            class="w-full max-w-[460px] bg-white p-6 md:p-10 rounded-3xl shadow-2xl shadow-gray-200/50 border border-gray-100 transition-all text-center"
        >
            <!-- Logo -->
            <div class="mb-6 flex justify-center">
                <a href="/" class="flex items-center gap-2.5 group">
                    <div
                        class="w-11 h-11 bg-[#ff6b6b] rounded-xl flex items-center justify-center text-white font-bold text-xl group-hover:scale-110 transition-transform shadow-lg shadow-red-200"
                    >
                        N
                    </div>
                    <span
                        class="text-2xl font-bold text-gray-800 tracking-tight"
                        >NexOne</span
                    >
                </a>
            </div>

            <!-- Icon -->
            <div
                class="w-16 h-16 bg-[#ff6b6b]/10 rounded-2xl flex items-center justify-center mx-auto mb-6 rotate-3"
            >
                <i class="fa-solid fa-envelope text-[#ff6b6b] text-2xl"></i>
            </div>

            <h1 class="text-2xl font-bold text-gray-800 mb-3 tracking-tight">
                Cek Email Kamu
            </h1>
            <p class="text-gray-500 text-sm leading-relaxed mb-8">
                Kami telah mengirimkan link verifikasi ke alamat email yang kamu
                daftarkan. Link tersebut berlaku selama 24 jam.
            </p>

            <button
                type="button"
                class="w-full py-3.5 bg-[#ff6b6b] text-white rounded-xl font-bold text-base hover:bg-[#ff5252] transition-all shadow-lg shadow-red-200 hover:shadow-xl hover:-translate-y-0.5 active:scale-[0.98]"
            >
                <i class="fa-solid fa-envelope-open mr-2"></i> Buka Email App
            </button>

            <div class="mt-8 pt-6 border-t border-gray-100">
                <p class="text-sm text-gray-500 mb-4">
                    Tidak menerima email? <a
                        href="#"
                        class="font-bold text-[#ff6b6b] hover:underline"
                        >Kirim ulang</a
                    >
                </p>
                <a
                    href="/auth/login"
                    class="inline-flex items-center text-sm font-medium text-gray-400 hover:text-[#ff6b6b] transition-colors"
                >
                    <i class="fa-solid fa-arrow-left mr-2"></i> Kembali ke Login
                </a>
            </div>
        </div>
    </div>
</Layout>
